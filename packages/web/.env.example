# Base URL of the mini-cloud service. Defaults to http://127.0.0.1:3000 when unset.
VITE_MINI_CLOUD_API_URL=http://127.0.0.1:3000

# Only needed when the service runs with MINI_CLOUD_TOKEN set. Sent as a bearer
# token on every request. Baked into the bundle at build time, so treat a build
# made with this set as being as secret as the token itself.
# VITE_MINI_CLOUD_TOKEN=
