/**
 * Empty here; the binary build replaces this module with one generated from
 * `migrations/`, because a single executable has no files beside it.
 */
export const EMBEDDED_MIGRATIONS: Readonly<Record<string, string>> = {};
